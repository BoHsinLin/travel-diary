import type { Place, Trip, TripDay, TripMember } from '../../contracts/entities';
import type { TripRepository } from '../interfaces/TripRepository';
export const DEMO_TRIP_ID = 'trip-001';
export const DEMO_PLACE_ID = 'place-004';
const trip: Trip = { id: DEMO_TRIP_ID, ownerId: 'user-001', title: '首爾 5 天 4 夜', destination: '首爾', timezone: 'Asia/Seoul', startDate: '2026-09-21', endDate: '2026-09-25', defaultPace: 'balanced', currency: 'KRW', currentDayId: 'day-002' };
const days: TripDay[] = [{ id: 'day-001', tripId: trip.id, date: '2026-09-21', title: '抵達首爾', sortOrder: 1 }, { id: 'day-002', tripId: trip.id, date: '2026-09-22', title: '宮殿與老城', sortOrder: 2 }, { id: 'day-003', tripId: trip.id, date: '2026-09-23', title: '市場與設計', sortOrder: 3 }];
const members: TripMember[] = [{tripId:trip.id,userId:'user-001',displayName:'林柏',role:'owner'},{tripId:trip.id,userId:'user-002',displayName:'安琪',role:'editor'},{tripId:trip.id,userId:'user-003',displayName:'小晴',role:'editor'},{tripId:trip.id,userId:'user-004',displayName:'阿哲',role:'viewer'}];
const places: Place[] = [{ id: 'place-001', name: '國立現代美術館 首爾館', category: 'museum', categoryLabel: '美術館', region: '鐘路區', travelMinutes: 10, suggestedDurationMinutes: 90, rating: 4.6 }, { id: 'place-002', name: '三清洞街區', category: 'neighborhood', categoryLabel: '街區', region: '鐘路區', travelMinutes: 13, suggestedDurationMinutes: 90, rating: 4.5 }, { id: 'place-003', name: 'Cafe Onion 安國店', category: 'cafe', categoryLabel: '咖啡', region: '鐘路區', travelMinutes: 16, suggestedDurationMinutes: 60, rating: 4.4 }, { id: DEMO_PLACE_ID, name: '北村韓屋村', category: 'heritage', categoryLabel: '歷史街區', region: '鐘路區', travelMinutes: 18, suggestedDurationMinutes: 70, rating: 4.7 }];
export class MockTripRepository implements TripRepository { async listTrips() { return [structuredClone(trip)]; } async getTrip(id: string) { return id === trip.id ? structuredClone(trip) : null; } async listDays(id: string) { return id === trip.id ? structuredClone(days) : []; } async listMembers(id:string){return id===trip.id?structuredClone(members):[]} async listPlaces(id: string) { return id === trip.id ? structuredClone(places) : []; } async getPlace(id: string, placeId: string) { return id === trip.id ? structuredClone(places.find((place) => place.id === placeId) ?? null) : null; } }
export const tripRepository = new MockTripRepository();
