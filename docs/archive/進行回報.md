# Figma 進行回報

時間戳：2026-08-26 16:52:36（Asia/Taipei，UTC+08:00）

## 本次確認

- 已閱讀並核對 `HANDOFF_FIGMA.md`、`FIGMA_PROGRESS.md` 與 Figma `Core Screens`。
- Figma 目前有 19 個頂層手機畫面，皆存在且 `placeholder = false`。
- 第三批六個畫面全部使用 Noto Sans TC；既有品牌畫面另使用 Noto Serif TC。
- 稽核前共有 13 個 Reaction，且第三批行程規劃流程完全未串接。
- `02 今日行程／地圖` 有兩個裝飾矩形超出畫框，但父畫面已裁切，判定為視覺裝飾，暫不調整。

## 本次完成修正

1. 串接 P0 景點加入行程流程：
   - 行程編排 `61:44` → 附近熱門 `61:45`
   - 附近熱門 `61:45` → 搜尋篩選 `61:46`
   - 搜尋結果 `61:46` → 加入設定 `61:47`
   - 加入設定 `61:47` → 衝突處理 `61:48`
   - 衝突處理 `61:48` → 行程編排 `61:44`
   - 航班重新排程 `61:49` → 行程編排 `61:44`

2. 補上第三批六個畫面的返回控制：
   - Node IDs：`73:52`、`73:55`、`73:58`、`73:61`、`73:64`、`73:67`
   - 尺寸均為 88×44 px，符合最低觸控尺寸。

3. 修正錯誤 Prototype 綁定：
   - 移除 `54:94`（20×20 toggle 圓點）的錯誤跳轉。
   - 改由 `54:95`「儲存旅程設定」按鈕返回旅程總覽。

4. 驗證結果：
   - P0 主路徑與返回路徑均可解析至同頁頂層 Frame。
   - 第三批已串接互動沒有小於 44×44 px 的目標。
   - 行程編排與加入行程畫面已重新截圖，未發現新增控制造成的遮擋或溢位。
   - `.figma-design-state.json` 已更新至 19 個畫面及最新 Prototype 狀態。

## 仍待完成

- 無衝突直接加入分支。
- 儲存中、成功、失敗、重試與 Undo 狀態。
- Button、Input、Itinerary Row、Status 的完整 component variants。
- 景點 CRUD、跨日拖放、暫存區批次安排、邀請旅伴與角色調整。
- 響應式規則、Light／Dark 逐頁驗證與 WCAG 2.2 AA。
- 整理正式 icon library，逐步取代仍存在的符號字元。

## 下一步建議

先完成 P0 的操作狀態與無衝突分支，再建立景點 CRUD 與拖放狀態元件；完成後重新執行 Prototype 死路、44 px 觸控目標、overflow 與深色模式稽核。
